/// Set to [true] when testing on Android Emulator with route playback.
/// Relaxes GPS filters and classifier thresholds so emulator mock locations
/// are accepted. Set to [false] before shipping to production.
const bool kDebugLocationMode = true;
